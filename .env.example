# OpenAI API Key (REQUIRED)
# Get your API key from https://platform.openai.com
OPENAI_API_KEY=your_openai_api_key_here

# Reddit API Configuration (REQUIRED for Vercel/Production)
# Reddit blocks unauthenticated requests from cloud infrastructure.
# To get these credentials:
# 1. Go to https://www.reddit.com/prefs/apps
# 2. Click "Create App" or "Create Another App"
# 3. Choose "script" as the app type
# 4. Fill in the required fields (name, description, redirect uri can be http://localhost)
# 5. Copy the client ID (under the app name) and secret
# 6. Add these to your Vercel environment variables

REDDIT_CLIENT_ID=your_reddit_client_id_here
REDDIT_CLIENT_SECRET=your_reddit_client_secret_here
