# OpenAI API Key (REQUIRED)
# Get your API key from https://platform.openai.com
OPENAI_API_KEY=your_openai_api_key_here

# Note: Reddit API credentials are NOT required!
# The app uses Reddit's public JSON API which doesn't need authentication.
# This makes setup much simpler - just add your OpenAI key and you're ready to go!
